# rtk (Rust Token Killer) — auto-wrap CLI commands for token savings
# Sourced from .bashrc
export PATH="$HOME/.local/bin:$PATH"

# === Git ===
alias git='rtk git'
alias gh='rtk gh'

# === File system ===
alias ls='rtk ls'
alias tree='rtk tree'
alias find='rtk find'
alias grep='rtk grep'
alias diff='rtk diff'
alias wc='rtk wc'

# === Docker / K8s ===
alias docker='rtk docker'
alias kubectl='rtk kubectl'

# === Dev tools ===
alias cargo='rtk cargo'
alias npm='rtk npm'
alias npx='rtk npx'
alias pnpm='rtk pnpm'
alias pip='rtk pip'
alias pytest='rtk pytest'
alias ruff='rtk ruff'
alias mypy='rtk mypy'
alias go='rtk go'

# === Web/Network ===
alias curl='rtk curl'
alias wget='rtk wget'

# === DB ===
alias psql='rtk psql'

# === JS ecosystem ===
alias jest='rtk jest'
alias vitest='rtk vitest'
alias next='rtk next'
alias tsc='rtk tsc'
alias lint='rtk lint'
alias playwright='rtk playwright'
alias prisma='rtk prisma'

# === Ruby ===
alias rspec='rtk rspec'
alias rubocop='rtk rubocop'
alias rake='rtk rake'

# === .NET ===
alias dotnet='rtk dotnet'

# === Cloud ===
alias aws='rtk aws'

# === Utility ===
alias env='rtk env'